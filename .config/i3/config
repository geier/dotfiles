# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# colors (somehow the order of these matters...)
set $baseoiii #002B36
set $baseoi #586E75
set $basei #93A1A1
set $baseii #EEE8D5
set $baseiii #FDF6E3
set $baseo #839496
set $baseoii #073642

set $yellow #B58900
set $orange #CB4B16
set $red #DC322F
set $magenta #D33682
set $violet #6C71C4
set $blue #268BD2
set $cyan #2AA198
set $green #859900

# class border background text indicator
client.focused $basei $baseo $baseoiii #31392F
client.focused_inactive $bor-in $baseoii $t-in #676767
client.unfocused $bor-in $baseoii $t-in #515151
client.urgent $bor-ur $back-urgen $t-ur #900000

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
# neo: x, qwertz: q
bind $mod+Shift+24 kill


# start dmenu (a program launcher)
# neo: a, qwertz: d
bind $mod+40 exec rofi -show combi
# tab
bind $mod+23 exec rofi -show window
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# neo: b; qwertz: n
bind $mod+57 exec /home/cg/bin/i3-rename-workspace


# change focus
# neo: n, qwertz: j
bind $mod+44 focus left
# neo: r, qwertz: k
bind $mod+45 focus down
# neo: t, qwertz: l
bind $mod+46 focus up
# neo: d, qwertz: ö
bind $mod+47 focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
# neo: N, qwertz: J
bind $mod+Shift+44 move left  
# neo: R, qwertz: K
bind $mod+Shift+45 move down
# neo: T, qwertz: L
bind $mod+Shift+46 move up
# neo: D, qwertz: Ö
bind $mod+Shift+47 move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
# neo: s, qwertz: h
bind $mod+43 split h

# split in vertical orientation
# neo: p, qwertz v
bind $mod+55 split v

# enter fullscreen mode for the focused container
# neo: e, qwertz: f
bind $mod+41 fullscreen

# change container layout (stacked, tabbed, toggle split)
# neo: i, qwertz: s
bind $mod+39 layout stacking
# neo: v, qwertz: w
bind $mod+25 layout tabbed
# neo: l, qwertz: e
bind $mod+26 layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
# neo: u, qwertz: a
bindsym $mod+38 focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym Mod4+1 workspace number 1
bindsym Mod4+2 workspace number 2
bindsym Mod4+3 workspace number 3
bindsym Mod4+4 workspace number 4
bindsym Mod4+5 workspace number 5
bindsym Mod4+6 workspace number 6
bindsym Mod4+7 workspace number 7
bindsym Mod4+8 workspace number 8
bindsym Mod4+9 workspace number 9
bindsym Mod4+0 workspace number 10
bindsym Mod4+F1 workspace number 11
bindsym Mod4+F2 workspace number 12
bindsym Mod4+F3 workspace number 13
bindsym Mod4+F4 workspace number 14
bindsym Mod4+F5 workspace number 15
bindsym Mod4+F6 workspace number 16
bindsym Mod4+F7 workspace number 17
bindsym Mod4+F8 workspace number 18
bindsym Mod4+F9 workspace number 19
bindsym Mod4+F10 workspace number 20

# move focused container to workspace
bindsym Mod4+Shift+1 move container to workspace number 1
bindsym Mod4+Shift+2 move container to workspace number 2
bindsym Mod4+Shift+3 move container to workspace number 3
bindsym Mod4+Shift+4 move container to workspace number 4
bindsym Mod4+Shift+5 move container to workspace number 5
bindsym Mod4+Shift+6 move container to workspace number 6
bindsym Mod4+Shift+7 move container to workspace number 7
bindsym Mod4+Shift+8 move container to workspace number 8
bindsym Mod4+Shift+9 move container to workspace number 9
bindsym Mod4+Shift+0 move container to workspace number 10
bindsym Mod4+Shift+F1 move container to workspace number 11
bindsym Mod4+Shift+F2 move container to workspace number 12
bindsym Mod4+Shift+F3 move container to workspace number 13
bindsym Mod4+Shift+F4 move container to workspace number 14
bindsym Mod4+Shift+F5 move container to workspace number 15
bindsym Mod4+Shift+F6 move container to workspace number 16
bindsym Mod4+Shift+F7 move container to workspace number 17
bindsym Mod4+Shift+F8 move container to workspace number 18
bindsym Mod4+Shift+F9 move container to workspace number 19
bindsym Mod4+Shift+F10 move container to workspace number 20

#
# reload the configuration file
# neo: ä, qwertz: c
bind $mod+Shift+54 reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
# #neo: c, qwertz: r
bind $mod+Shift+27 restart
# exit i3 (logs you out of your X session)
# neo:l, qwertz: e
bind Mod4+Shift+26 exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym n resize shrink width 10 px or 10 ppt
        bindsym r resize grow height 10 px or 10 ppt
        bindsym t resize shrink height 10 px or 10 ppt
        bindsym d resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

#neo: c, qwertz: r
bind $mod+27 mode "resize"
new_window 1pixel

# Move workspace to left, as long as we only have two monitors this is enough (as monitor order is cyclic) (

# sadly this is taken already...
#bind $mod+Shift+43 move workspace to output right
# neo: o, qwertz: g
bind $mod+Shift+42 move workspace to output left

## Put this window on the presentation output.
#bindsym Mod4+Shift+o move workspace to output DFP11
#bindsym Mod4+o move container to output DFP11

font pango:Ubuntu Mono 12

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
       status_command /home/cg/bin/my_i3status.sh
       position top
       font pango:Droid Sans Bold


	colors {
		background $baseoii
		statusline $baseiii
		separator $baseii
		#border #background #text
		focused_workspace $basei $baseo $baseoii
		active_workspace $baseoiii $baseoiii $baseii
		inactive_workspace $baseoii $baseoii $baseo
		urgent_workspace $baseoiii $red $baseoii
	}
}

for_window [instance="screenkey"] floating enable none


# start other applications
exec gnome-settings-daemon &
exec nm-applet &
exec glipper &
bindsym Mod4+Ctrl+L exec i3lock -i ~/Pictures/lockscreen.png
exec --no-startup-id feh --bg-fill /home/cg/Pictures/Wallpapers/default.jpg
exec_always xautolock -time 10 -locker 'i3lock -i ~/Pictures/lockscreen.png'

# vim:filetype=i3
